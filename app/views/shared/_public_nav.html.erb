<!-- Public Navigation -->
<nav class="bg-white shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center gap-3">
        <%= link_to root_path, class: "flex items-center gap-3" do %>
          <%= image_tag "Logo Blue.png", alt: "HabitBeaver Logo", style: "height: 40px; width: auto;" %>
        <% end %>
      </div>
      <div class="flex items-center gap-4">
        <%= link_to "Privacy Policy", privacy_path, class: "text-sm hover:opacity-70", style: "color: #657b84;" %>
        <%= link_to "Terms of Service", terms_path, class: "text-sm hover:opacity-70", style: "color: #657b84;" %>
        <% if user_signed_in? %>
          <%= link_to "Dashboard", dashboard_path, class: "px-4 py-2 rounded-lg text-sm font-semibold text-white transition hover:opacity-90", style: "background-color: #1d3e4c;" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "px-4 py-2 rounded-lg text-sm font-semibold text-white transition hover:opacity-90", style: "background-color: #1d3e4c;" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
